{
  "name": "saas-boilerplate-frontend",
  "private": true,
  "scripts": {
    "postinstall": "npm install pnpm --save-dev && npx pnpm install --force && npx pnpm exec nx run webapp:graphql:generate-types",
    "dev": "npx pnpm --filter webapp run dev",
    "build": "npx pnpm --filter webapp run build",
    "preview": "npx pnpm --filter webapp run preview"
  },
  "devDependencies": {
    "pnpm": "^10.11.0"
  }
}
